def modify_without_global():
    local_var = "I am local"  # Local variable inside the function
    print(local_var)  # Printing the local variable

modify_without_global()  # Calling the function outside itself
